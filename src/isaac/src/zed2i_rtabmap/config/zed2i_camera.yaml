/**:
  ros__parameters:
    general:
      serial_number: 35134495
      grab_resolution: HD720
      grab_frame_rate: 30
      pub_resolution: NATIVE
      pub_frame_rate: 30.0

    sensors:
      sensors_pub_rate: 200.0
      sensors_image_sync: false

    depth:
      depth_mode: PERFORMANCE
      depth_stabilization: 1
      point_cloud_freq: 15.0

    pos_tracking:
      pos_tracking_enabled: true        # Enable ZED odometry for RTABMap
      publish_map_tf: false             # RTABMap handles map->odom TF
      area_memory: false
      reset_odom_with_loop_closure: false

    mapping:
      mapping_enabled: false            # RTABMap handles mapping

    object_detection:
      od_enabled: false

    body_tracking:
      bt_enabled: false
